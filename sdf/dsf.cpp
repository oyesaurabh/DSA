#include<bits/stdc++.h>
using namespace std;
long long mod=1e9+7;
long long solve(vector<int>&a){
    int n=a.size();
    for(int i=0;i<n;i++)
        for(int j=0;j<n;j++)
            if()
}
int main(){
    int n;cin>>n;
    vector<int> a(n);
    for(auto &it:a)
        cin>>it;
    cout<<solve(a);
    return 0;
}